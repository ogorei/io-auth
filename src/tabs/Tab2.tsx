import { IonContent, IonHeader, IonPage, IonTitle, IonToolbar } from '@ionic/react';
import Register from './Tab1';

const Tab2: React.FC = () => {
  return (
      <Register />
  );
};

export default Tab2;
